const config = {
    endpoint: "http://localhost:8080"
    // endpoint: 'https://blog-project-backend-9ma0.onrender.com'
    // endpoint: 'https://blog-project-backend-production.up.railway.app'
}

window.config = config